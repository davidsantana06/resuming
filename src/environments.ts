import 'dotenv/config';

export const { JWT_SECRET } = process.env;

export const { PORT = 3000 } = process.env;

export const { SOURCE_CODE_URL } = process.env;
export const { RELEASES_URL } = process.env;
